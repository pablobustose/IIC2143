<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <title></title>
</head>
<body>
  <div class="container">
    <h2></h2>

    <%= form_for [@room, @room_message], html: {class: "form"} do |f| %>
      <% if @room_message.errors.any? %>
        <div class="alert alert-danger">
          <h4><%= pluralize(@room_message.errors.count, "error") %> que no permiten crear el mensaje:</h4>
          <ul>
            <% @room_message.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :content, class: "control-label" %>
        <%= f.text_field :content, class: "form-control" %>
      </div>

      <%= f.submit "Enviar mensaje", class: "btn btn-primary" %>
    <% end %>

    <a href="<%= request.referrer || root_path %>" class="btn btn-secondary">Volver</a>
  </div>

  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>